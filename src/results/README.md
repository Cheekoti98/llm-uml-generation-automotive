Aggregated evaluation results and figures.
Once after the LLMs(Llama 3.1 and GPT-4o) have created the UML diagrams, these generated UML models are compared against the reference UML models.
Different evaluation methods utilized are:
  Graph edit distance(GED)
  CodeBLEU
  Semantic consistency score using LLM
  Human evaluation
